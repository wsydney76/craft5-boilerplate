{# @param entry Entry #}
{% embed '_layouts/partials/box.twig' %}
    {% block content %}
        {# author.name will the the user name, if no full name is present #}
        {{ 'Created by'|t }} {{ entry.author.name }}, {{ entry.postDate|date('short') }}
    {% endblock %}
{% endembed %}