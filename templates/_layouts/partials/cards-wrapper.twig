{# A consistent responsive card layout #}

{# Grid layout depending on screen width #}
{# max-w-... center cards if there is only one column #}
<div
    class="mx-auto grid max-w-[450px] grid-cols-1 gap-4 md:mx-0 md:max-w-none md:grid-cols-2 lg:grid-cols-3"
>
    {% block cards %}

    {% endblock cards %}
</div>
